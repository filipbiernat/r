import React from "react";

const TitleBar: React.FC = () => {
    return (
        <div className="bg-light p-3 text-center">
            <h1>Moje zebrane dane</h1>
        </div>
    );
};

export default TitleBar;
